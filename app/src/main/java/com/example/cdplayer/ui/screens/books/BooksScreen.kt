package com.example.cdplayer.ui.screens.books

import androidx.compose.runtime.Composable

/**
 * PDF 파일 목록 화면
 *
 * TODO: 구현 예정
 * - 기기 내 PDF 파일 스캔 및 목록 표시
 * - 파일 선택 시 PdfViewerScreen으로 이동
 * - SAF(Storage Access Framework)로 외부 파일 불러오기
 * - 마지막 읽은 페이지 / 진행률 표시
 * - 최근 읽은 순 정렬
 */
@Composable
fun BooksScreen(
    onNavigateToPdfViewer: (String) -> Unit
) {
    // TODO: implement
}
